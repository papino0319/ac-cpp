#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using P = pair<ll,ll>;
#define rep(i,n) for (ll i = 0; i < (ll)(n); ++i)

int main() {
    // if require setprecision
    //cout << std::fixed << std::setprecision(15);

    ll ans = 0;
    cout << ans << endl;
}
